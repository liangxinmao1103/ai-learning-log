# AI项目研究计划变动
## 原因：key需要购买且价格不菲
## 对本地跑ai模型的一些感思
   先接着本地模型用，学习过程不能断，依旧延续着之前的路线走，只不过替代了API（当时没想到key是需要花费的）
   学习本身就是一个迭代升级的过程，先行，行且将至。
## 初步探索：1，这个所谓的本地模型调用，直接用LM省了大麻烦，应该是和openai的服务一致，gpt改一下原调用API的代码即可，这样本地输出就可行了吧。
